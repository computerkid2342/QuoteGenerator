import React from 'react'; //react components
import ReactDOM from 'react-dom'; //hook onto the element the react application will be contained in

import App from './App'; //main component

ReactDOM.render(<App />, document.getElementById('root') ); // render App and hook onto the "root"